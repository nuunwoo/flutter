name: widget
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  dependencies:
    path: ../dependencies

  models:
    path: ../models

  screen:
    path: ../screen

  utility:
    path: ../utility

flutter:
  uses-material-design: true

  assets:
    - ../../assets/images/
    - ../../assets/images/menu/
